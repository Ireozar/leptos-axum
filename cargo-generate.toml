[placeholders]
nightly = { prompt = "Use nightly features?", choices = ["No", "Yes"], default = "Yes", type = "string"}

[conditional.'nightly == "No"']
ignore = ["rust-toolchain.toml"]

[template]
exclude = ["public/*"]
